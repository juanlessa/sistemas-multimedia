function pot = Potencia(v)
    %v = vetor de amostras
    pot = v*v'/length(v); 

    %nomes uteis
    %t = intervalo de amostra do sinal
    %Ta = intervalo de tempo entre cada amostra
    %T = periodo do sinal
  end
